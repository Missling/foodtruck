<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="initial-scale=1.0, user-scalable=no">
    <meta charset="utf-8">
    <title>Simple markers</title>
  </head>
  <body>
    <div id="map"></div>
    <script async defer
        src="https://maps.googleapis.com/maps/api/js?signed_in=true&callback=initMap"></script>

    <% @foodtrucks.each do |foodtruck| %>
      <div class='block'>
        <div>Name: <%= foodtruck.name %></div>
        <div>Address: <%= foodtruck.address %></div>
        <% if foodtruck.description %>
          <div>Menus: <%= foodtruck.description %></div>
        <% end %>
      </div>
    <% end %>
  </body>
</html>